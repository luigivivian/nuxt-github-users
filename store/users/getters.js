
const getters = {
    user(state) {
      return state.user
    },
    repos(state){
      return state.repos;
    },
    starreds(state){
      return state.repos;
    }
  };
  
export default getters;